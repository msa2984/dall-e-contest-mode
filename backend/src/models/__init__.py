class ContestImage(object):
    def __init__(self, id, url, user, prompt, votes):
        self.id = id
        self.url = url
        self.user = user
        self.prompt = prompt
        self.votes = votes
